# Backend API Configuration
# Change the port below to match your backend API port (default: 8000)
VITE_API_PORT=8000
VITE_API_HOST=127.0.0.1
VITE_API_PROTOCOL=http

# Full Base URL override (optional - overrides protocol, host, and port if set):
# VITE_API_URL=http://localhost:8000
